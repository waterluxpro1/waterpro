'use client'

import { Container } from '@/shared/ui/Container'
import { Loading } from '@/widgets/Loading/Loading'

const GoodIsLoading = () => (
	<Container>
		<Loading />
	</Container>
)

export default GoodIsLoading